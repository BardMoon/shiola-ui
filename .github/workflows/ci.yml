# .github/workflows/ci.yml
name: Build

on:
  push:
    branches:
      - release

jobs:
  build-package:
    runs-on:
      - ubuntu-latest
    steps:
      - run: pnpm install
      - run: pnpm build

  deploy-preview:
    runs-on:
      - ubuntu-latest
    steps:
      - run: pnpm install
      - run: pnpm build:preview
